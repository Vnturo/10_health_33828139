<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Log a Workout</title>
    <link rel="stylesheet" type="text/css" href="/style.css">
    <style>
        body {
            font-family: Arial, Helvetica, sans-serif;
        }

        .main-container {
            max-width: 600px;
            margin: 40px auto;
        }

        .card {
            background: white;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }

        .card-purple {
            border-left: 7px solid rgb(102, 80, 153);
        }

        label {
            color: #333;
            font-size: 16px;
            font-weight: bold;
            margin-bottom: 5px;
            display: inline-block;
        }
        
        input[type="text"], 
        input[type="number"] {
            background-color: #f9f9f9;
            transition: border-color 0.3s;
            font-family: Arial, Helvetica, sans-serif;
        }

        input[type="text"]:focus, 
        input[type="number"]:focus {
            border-color: rgb(102, 80, 153);
            outline: none;
            background-color: white;
        }
    </style>
</head>
<body>
    <header>
        <h1>Health Tracker</h1>
        <nav>
    <ul>
        <li><a href="<%= baseUrl %>/">Home</a></li>
        <li><a href="<%= baseUrl %>/about">About Us</a></li>
        <li><a href="<%= baseUrl %>/workouts/list">My Workouts</a></li>
        <li><a href="<%= baseUrl %>/goals">My Goals</a></li>
        <li><a href="<%= baseUrl %>/users/logout">Logout</a></li>
    </ul>
</nav>
    </header>

    <main class="main-container">
        
        <section class="card card-purple">
            <h2 style="color: rgb(102, 80, 153); font-size: 24px; margin-bottom: 20px; border-bottom: 1px solid #eee; padding-bottom: 10px;">Log a New Workout</h2>

            <% if (typeof errors !== 'undefined') { %>
                <div class="error" style="background-color: #ffdddd; border-left: 5px solid #f44336; padding: 10px; margin-bottom: 20px;">
                    <% errors.forEach(function(error) { %>
                        <p style="margin: 0; color: #d32f2f; font-family: Arial, sans-serif;"><%= error.msg %></p>
                    <% }); %>
                </div>
            <% } %>

            <form action="<%= baseUrl %>/workouts/added" method="POST">
                <div style="margin-bottom: 20px;">
                    <label for="name">Workout Name:</label><br>
                    <input type="text" id="name" name="name" required style="padding: 12px; width: 100%; box-sizing: border-box;">
                </div>

                <div style="margin-bottom: 20px;">
                    <label for="duration">Duration (minutes):</label><br>
                    <input type="number" id="duration" name="duration" min="1" required style="padding: 12px; width: 100%; box-sizing: border-box;">
                </div>

                <div style="margin-bottom: 25px;">
                    <label for="calories">Calories Burned:</label><br>
                    <input type="number" id="calories" name="calories" min="1" required style="padding: 12px; width: 100%; box-sizing: border-box;">
                </div>

                <div style="display: flex; gap: 10px; align-items: center;">
                    <button type="submit" style="font-size: 16px; padding: 12px 30px; background-color: rgb(102, 80, 153); color: white; border: none; border-radius: 4px; cursor: pointer;">Add Workout</button>
                    <a href="/workouts/list" style="color: gray; font-size: 14px; text-decoration: underline;">Cancel</a>
                </div>
            </form>
        </section>
    </main>

    <footer>
        <p>&copy; 2025 Health Tracker</p>
    </footer>
</body>
</html>