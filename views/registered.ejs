<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration Successful</title>
    <link rel="stylesheet" type="text/css" href="/style.css">
    <style>
        body {
            font-family: Arial, Helvetica, sans-serif;
        }

        .main-container {
            max-width: 600px;
            margin: 50px auto;
            text-align: center;
        }

        .card {
            background: white;
            padding: 40px;
            border-radius: 8px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }

        .card-purple {
            border-left: 7px solid rgb(102, 80, 153);
        }

        .success-box {
            background-color: #d4edda;
            border-left: 5px solid #28a745;
            padding: 20px;
            margin-bottom: 30px;
            text-align: left;
            border-radius: 4px;
        }

        .btn-login {
            display: inline-block;
            background-color: rgb(102, 80, 153);
            color: white !important;
            padding: 15px 30px;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            font-size: 18px;
            transition: background-color 0.3s;
        }

        .btn-login:hover {
            background-color: rgb(75, 50, 120);
        }
    </style>
</head>
<body>
    <header>
        <h1>Health Tracker</h1>
        <nav>
    <ul>
        <li><a href="<%= baseUrl %>/">Home</a></li>
        <li><a href="<%= baseUrl %>/about">About Us</a></li>

        <% if (typeof user !== 'undefined' && user) { %>
            <li><a href="<%= baseUrl %>/workouts/list">My Workouts</a></li>
            <li><a href="<%= baseUrl %>/goals">My Goals</a></li>
            <li><a href="<%= baseUrl %>/users/logout">Logout</a></li>
        <% } else { %>
            <li><a href="<%= baseUrl %>/users/register">Register</a></li>
            <li><a href="<%= baseUrl %>/users/login">Login</a></li>
        <% } %>
    </ul>
</nav>
    </header>

    <main class="main-container">
        <section class="card card-purple">
            <h2 style="color: rgb(102, 80, 153); font-size: 24px; margin-bottom: 20px;">Registration Successful!</h2>
            
            <div class="success-box">
                <p style="margin: 5px 0;">Hello <strong><%= first %> <%= last %></strong>,</p>
                <p style="margin: 5px 0;">You have successfully registered with the username: <strong><%= username %></strong></p>
                <p style="margin: 5px 0;">Your confirmation email was sent to: <strong><%= email %></strong></p>
            </div>
            
            <p style="margin-bottom: 20px; color: #666;">You are now ready to start tracking your fitness journey.</p>

            <a href="/users/login" class="btn-login">Log In Now</a>
        </section>
    </main>

    <footer>
        <p>&copy; 2025 Health Tracker</p>
    </footer>
</body>
</html>