<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Welcome Back</title>
    <link rel="stylesheet" type= "text/css" href="/style.css">
</head>
<body>
    <header>
        <h1>Health Tracker Dashboard</h1>
        <nav>
    <ul>
        <li><a href="<%= baseUrl %>/">Home</a></li>
        <li><a href="<%= baseUrl %>/about">About Us</a></li>

        <% if (typeof user !== 'undefined' && user) { %>
            <li><a href="<%= baseUrl %>/workouts/list">My Workouts</a></li>
            <li><a href="<%= baseUrl %>/goals">My Goals</a></li>
            <li><a href="<%= baseUrl %>/users/logout">Logout</a></li>
        <% } else { %>
            <li><a href="<%= baseUrl %>/users/register">Register</a></li>
            <li><a href="<%= baseUrl %>/users/login">Login</a></li>
        <% } %>
    </ul>
</nav>
    </header>

    <main>
        <section>
            <h2>Welcome back, <%= user.first_name %>!</h2>
            <p>You are now logged in as <strong><%= user.username %></strong>.</p>
            
            <div style="margin-top: 30px; display: flex; gap: 20px; justify-content: center;">
    
        <a href="/workouts/add" class="btn-add">Log a Workout</a>

        <a href="/goals" class="btn-add">View Goals</a>
            </div>
        </section>
    </main>

    <footer>
        <p>&copy; 2025 Health Tracker</p>
    </footer>
</body>
</html>